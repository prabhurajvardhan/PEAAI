/**
 * Canvas Engine Module
 * 
 * Exports the CanvasEngine class implementing ICanvas interface
 * for HTML5 canvas management with DPR support.
 */

export { CanvasEngine, ICanvas, CanvasConfig } from './canvas';
export type { ISize } from '../types';
