/**
 * Pixel Buffer Module
 * 
 * Exports the PixelBuffer class implementing IPixelBuffer interface
 * for efficient pixel data manipulation.
 */

export { PixelBuffer, IPixelBuffer } from './buffer';
export type { IColor, IPixelBatch, BlendMode } from '../types';
