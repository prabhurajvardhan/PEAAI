# Backend & AI Engineer — BA-006

> Story Generation Engineer (historical) → **Deployment Integration Engineer (active)**

---

## Employee ID

BA-006

---

## Role

**Active:** Backend & AI / Deployment Integration Engineer
**Historical:** Story Generation Engineer (M08 — completed)

---

## Team

Backend & AI Engineering

---

## Status

🟠 Active — V0.1 Staging Deployment Readiness (Vercel + Render)
_(Historical task: ✅ Complete — Story Generation Pipeline)_

---

## Module

**Active assignment scope:** Cross-cutting deployment integration (frontend + backend), respecting module ownership (M09 = BA-003/BA-004/BA-005; M01/M07 = UI-*).
**Historical module:** M08 AI Engine

---

## Assignment Status Summary

| Task | Role | Status |
|------|------|--------|
| Story Generation Pipeline (historical) | Story Generation Engineer | ✅ Completed (2026-08-03) |
| V0.1 Staging Deployment Readiness — Vercel + Render | Deployment Integration Engineer | 🟠 Active (assigned by CA) |

---

## Active Assignment — V0.1 Staging Deployment Readiness (Vercel + Render)

> Assigned by the Chief Architect (CA). Staging-readiness only — **do NOT deploy to the production domain.**
> Make the repository technically ready for a temporary staging deployment where **Frontend → Vercel** and **Backend → Render**, and verify the independently hosted frontend can communicate with the independently hosted backend.

### Objective

Prepare PEAAI V0.1 for a safe staging deployment (NOT production):

```
Vercel  → React frontend
Render  → FastAPI backend → PostgreSQL / Redis / required storage
```

The goal is to make the current repository technically ready for a temporary staging deployment and verify that the independently hosted frontend can communicate correctly with the independently hosted backend.

### Repository Context (audited by CA — verify, do not assume)

The following was confirmed from the actual repository at assignment time. BA-006 MUST still audit and verify each item, and correct anything that has changed.

**Backend (FastAPI, M09 — owned by BA-003/BA-004/BA-005):**
- Entry point: `backend/api/main.py` → `app = FastAPI(...)`. Routers: `auth`, `users`, `conversations`, `memories`, `sessions`, `storage`, `websocket`.
- **Health endpoints already exist:** `GET /` and `GET /health` (return `name`/`version`/`status`, **no secrets**). Task #13: VERIFY these are sufficient for Render's health check — do NOT add a duplicate endpoint. Only add/improve if inadequate (e.g., a liveness probe that does not expose secrets).
- Config: `backend/api/config.py` (pydantic `BaseSettings`, `env_file=".env"`). Existing vars: `APP_NAME`, `APP_VERSION`, `DEBUG`, `DATABASE_URL` (default `postgresql://postgres:postgres@localhost:5432/peaai`), `SECRET_KEY` (default `your-secret-key-change-in-production` — INSECURE), `ALGORITHM`, `ACCESS_TOKEN_EXPIRE_MINUTES`, `REFRESH_TOKEN_EXPIRE_DAYS`, `PASSWORD_RESET_TOKEN_EXPIRE_HOURS`, `RATE_LIMIT_PER_MINUTE`, `RATE_LIMIT_PER_HOUR`, `ALLOWED_ORIGINS` (default `["*"]` — permissive), `WEBSOCKET_HEARTBEAT_*`, `SESSION_MAX_CONCURRENT`, `SESSION_TTL_HOURS`, `CDN_BASE_URL`, `STORAGE_PATH` (default `./storage`), `STORAGE_MAX_*`, `REDIS_URL` (default `redis://localhost:6379/0`).
- CORS already env-driven: `main.py` uses `settings.ALLOWED_ORIGINS` with `allow_credentials=True`. NOTE: `allow_credentials=True` + `allow_origins=["*"]` is invalid per the CORS spec — staging MUST set `ALLOWED_ORIGINS` to the explicit Vercel URL.
- **No uvicorn run command / Procfile / runtime.txt exists.** Render start command must be defined to bind `0.0.0.0:$PORT` (e.g. `uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT`, run from repo root so `backend.*` imports resolve). Verify import path works.
- Alembic: `backend/alembic.ini` (`script_location=migrations`, `sqlalchemy.url` commented out → loaded from env). `backend/migrations/env.py` reads `os.getenv("DATABASE_URL", "...localhost...")`. Run migrations from `backend/`: `alembic upgrade head`. Document the Render pre-deploy/startup migration procedure.
- Redis: `backend/session/store.py` defines `RedisSessionStore` AND `InMemorySessionStore`. **`backend/session/manager.py` defaults to `InMemorySessionStore()`** — `REDIS_URL` is configured but Redis is NOT actually wired at runtime in V0.1. Document this: external Redis is not strictly required for V0.1 staging (sessions are in-memory); do NOT redesign to wire Redis unless absolutely required. `REDIS_URL` is documented for future use.
- Storage: `backend/storage/manager.py` writes user files to the **local filesystem** (`STORAGE_PATH=./storage`, `os.makedirs` + `aiofiles` write). **Render web-service disk is ephemeral** — uploaded files are lost on redeploy. Document this limitation; do NOT redesign storage. Recommend the lightest safe staging approach (e.g., accept ephemeral storage for staging and document data loss, or note `CDN_BASE_URL` / external storage as a future option).
- AI/model: `backend/ai/llm/config.py` reads `LLM_API_KEY`, `OLLAMA_BASE_URL`, `OPENAI_ORGANIZATION` from env. Auth: `backend/auth/jwt_handler.py` uses `SECRET_KEY`/`ALGORITHM`.

**Frontend (M01/M07 — owned by UI-*):**
- Deployable frontend app shell: `src/app/` (`index.html`, `main.tsx`, `App.tsx`, `package.json` `@peaai/app-shell`). This is the **Vercel root candidate**.
- **V0.1 has NO working production frontend build pipeline:** `src/app/package.json` has no `build` script and no `vite` build tooling; root `package.json` `build` is `tsc` (broken — outputs to `dist/`, no HTML bundling). A Vercel build requires a build command + output directory. BA-006 must determine the exact Vercel config and the required build pipeline.
- **Only one HTTP/API client exists: `src/components/auth/api.ts`.** It reads `process.env?.VITE_API_URL` (NOT the canonical Vite `import.meta.env.VITE_API_URL`), so `VITE_API_URL` is currently **ineffective** and it falls back to the relative `/api/v1` — which will NOT reach a cross-origin Render backend from Vercel. Task #5 requires this env wiring to actually work.
- **No WebSocket client in the frontend** (no `new WebSocket` / `ws://` in `src/`). Backend `/ws` exists. Task #6: document that V0.1 has no frontend WS client; staging WS verification is backend-only (e.g., `wscat` to `wss://<render>/ws`). Document the WS URL config for when a frontend client exists (derive `ws`/`wss` from the configured backend URL; do not hardcode).
- No `.env` / `.env.example` exists. `.gitignore` ignores `.env` (good).

**CI / deployment config:**
- `.github/workflows/ci.yml` exists (backend gating = 177 tests; frontend jest/vitest gating jobs). BA-006 must NOT break CI. Run the backend tests + relevant frontend checks before finishing.
- No `vercel.json`, `render.yaml`, `Procfile`, `runtime.txt`, or deployment docs exist. Deployment docs should go in `docs/engineering/DEPLOYMENT.md` (matches the existing `docs/engineering/CI.md` location).

### Tasks

BA-006 must:

1. Audit the current repository structure and determine the correct Vercel frontend root/build configuration.
2. Audit the FastAPI backend and determine the correct Render start command.
3. Verify the backend can bind to `0.0.0.0:$PORT`.
4. Establish a clean frontend → backend configuration using environment variables.

   Preferred architecture:
   - Frontend: `VITE_API_URL=<Render backend URL>`
   - Backend: `ALLOWED_ORIGINS=<Vercel frontend URL>`

   Do NOT hardcode deployment URLs into application source code.

5. Audit all HTTP/API calls and ensure they use the configured backend URL rather than `localhost` or hardcoded development URLs. (See repository context: `src/components/auth/api.ts` is the only HTTP client and its `VITE_API_URL` access is currently ineffective — it uses `process.env` instead of `import.meta.env`.)
6. Audit WebSocket connections.
   - Development: `ws://...`
   - Staging: `wss://<render-backend>/ws`
   Use environment/configuration rather than hardcoding production URLs. (See repository context: V0.1 has no frontend WS client — document this.)
7. Audit CORS and configure it so the staging frontend can communicate with the staging backend. (CORS is already env-driven via `ALLOWED_ORIGINS`; staging must set it to the explicit Vercel URL — `["*"]` is invalid with `allow_credentials=True`.)
8. Audit backend environment variables. Identify ALL variables required for staging, including: database URL, secret key, Redis URL, allowed origins, storage configuration, AI/model configuration, authentication configuration, and any other required runtime variables discovered from the actual code.
9. Ensure development fallback values do NOT accidentally become production/staging secrets. Never commit: API keys, passwords, JWT secrets, database credentials, private tokens. (See repository context: insecure defaults for `SECRET_KEY`, `DATABASE_URL` (localhost), `REDIS_URL` (localhost), `ALLOWED_ORIGINS` (`["*"]`) — staging MUST override all of these.)
10. Audit PostgreSQL configuration. Ensure the backend does NOT depend on localhost PostgreSQL when deployed to Render. Verify the correct migration/startup procedure using the existing Alembic configuration (`backend/alembic.ini`, `backend/migrations/env.py` — both read `DATABASE_URL` from env).
11. Audit Redis configuration. Ensure the deployed backend can use an external Redis service rather than assuming localhost Redis. (See repository context: V0.1 sessions default to `InMemorySessionStore` — document whether Redis is actually required for V0.1 staging.)
12. Audit file/storage handling. Determine whether V0.1 currently writes persistent user data/files to the local filesystem. If so, document whether this is safe on Render and identify the required staging storage approach. Do NOT redesign the storage architecture unless absolutely required for staging. (See repository context: `backend/storage/manager.py` writes to local `./storage` — ephemeral on Render.)
13. Add or improve a backend health endpoint ONLY if an appropriate health endpoint does not already exist. The health check must not expose secrets. (See repository context: `GET /health` already exists and is secret-free — verify it; do not duplicate.)
14. Verify frontend/backend integration locally before deployment. Test: Frontend → API request → Backend → Database/required services. Also test WebSocket connectivity if implemented in V0.1.
15. Create/update deployment documentation explaining:
    - Vercel root directory, build command, output directory
    - Render root directory, start command
    - required environment variables
    - frontend API URL configuration
    - backend CORS configuration
    - WebSocket URL configuration
    - database requirements
    - Redis requirements
    - storage requirements
    - staging deployment procedure
    - verification procedure
16. Do NOT deploy to the real PEAAI production domain. This task is staging-readiness only.

### Architectural Constraints

- Do not redesign PEAAI architecture.
- Do not modify unrelated modules.
- Do not change application behavior unnecessarily.
- Do not introduce unnecessary dependencies.
- Do not hardcode staging or production URLs.
- Do not commit secrets.
- Do not replace existing services without architectural justification.
- Preserve the existing frontend/backend separation.
- Follow existing `docs/interfaces/INTERFACES.md` contracts.
- Follow existing `docs/modules/MODULES.md` ownership rules.
- Use environment variables for all deployment-specific configuration.
- Do not modify another employee's module to fix an UNRELATED issue.
- **If a required change belongs to another module, document it and report it instead of silently taking ownership.** This includes:
  - `backend/api/config.py`, `backend/api/main.py` (M09 — BA-003): deployment config is in scope, but app logic is not.
  - `src/components/auth/api.ts` (M01 — UI-003): the `VITE_API_URL` env-access fix is deployment-config wiring, not app logic — apply the minimal env-access correction and **explicitly document + report it as a cross-module change** in `BA-006.md` and the PR.
  - `src/app/*` (M01 — UI-009): if a Vite build pipeline is required for a Vercel build, add ONLY the minimal build config (`vite.config.ts`, `build` script, `vite` devDependency) needed for staging, flag it as a cross-module deployment-config change, and report it to the CA. Do NOT change app runtime behavior.
- All cross-module changes must be minimal, deployment-config-only, and transparently documented + reported (never silent).

### Dependencies (inspect before implementation — use the actual repository as source of truth)

- The existing CI workflow (`.github/workflows/ci.yml`).
- Backend API entry points (`backend/api/main.py`, `backend/api/routers/`).
- Frontend application entry points (`src/app/index.html`, `src/app/main.tsx`).
- API clients/services (`src/components/auth/api.ts`).
- WebSocket implementation (`backend/websocket/`).
- Environment/configuration files (`backend/api/config.py`, `backend/ai/llm/config.py`).
- Database configuration and migrations (`backend/database/__init__.py`, `backend/alembic.ini`, `backend/migrations/env.py`).
- Redis configuration (`backend/session/store.py`, `backend/session/manager.py`, `backend/api/config.py`).
- Storage implementation (`backend/storage/manager.py`, `backend/storage/cdn.py`).

Do not assume any of these components exist exactly as described above. Use the actual repository as the source of truth.

### Acceptance Criteria

The task is complete only when:

- [ ] Vercel frontend deployment configuration is clearly defined.
- [ ] Render backend deployment configuration is clearly defined.
- [ ] Frontend can obtain the backend URL through environment configuration.
- [ ] No deployment URL is hardcoded into application source.
- [ ] API requests correctly target the configured backend.
- [ ] WebSocket configuration supports the staging backend.
- [ ] CORS supports the staging frontend.
- [ ] Required backend environment variables are documented.
- [ ] No secrets are committed.
- [ ] PostgreSQL deployment requirements are documented.
- [ ] Redis deployment requirements are documented.
- [ ] Storage persistence limitations are documented.
- [ ] Backend health check is verified.
- [ ] Frontend → backend integration is tested locally.
- [ ] Existing CI tests still pass.
- [ ] Deployment documentation is complete.
- [ ] No unrelated application functionality is changed.

### Required Deliverables

- Update: `docs/employees/BA-006.md` (this document — keep status/progress here).
- Create/update deployment documentation at `docs/engineering/DEPLOYMENT.md` (matches existing `docs/engineering/CI.md` location).
- Add only the deployment-config files necessary for staging (e.g. `vercel.json`, `render.yaml`, `.env.example` for frontend and backend) — these are deployment config, NOT application logic, and must contain NO secrets and NO hardcoded staging/production URLs.
- Only modify files necessary for this task.
- Before finishing, run the relevant tests/build/lint checks (backend `python -m pytest backend/` must still pass; do not break CI gating jobs).
- Then update `BA-006.md` with: task status, files changed, tests performed, deployment readiness status, known limitations, and remaining external configuration required for Vercel/Render.
- Create a PR containing only this assignment's files.
- Do NOT deploy to the production domain.

### OpenHands Employee Prompt

```
You are BA-006, Backend & AI / Deployment Integration Engineer of PEAAI.

Your new assignment is V0.1 Staging Deployment Readiness for Vercel + Render.

Read docs/employees/BA-006.md first and follow the active assignment.

Then read the required architecture, module, interface, dependency, CI, frontend, and backend documentation.

Audit the actual repository before making changes.

Your goal is to prepare PEAAI V0.1 for:

Vercel
→ React frontend
→ Render backend
→ PostgreSQL / Redis / required storage

Use environment variables for all deployment-specific configuration.

Do not hardcode URLs or secrets.

Do not redesign the architecture.

Do not modify unrelated modules.

Test frontend → backend communication and WebSocket connectivity where applicable.

Run all relevant tests and CI-compatible checks.

Document the exact Vercel and Render configuration required.

Do not deploy to the production domain.

When complete, update docs/employees/BA-006.md, commit only task-related files, create a PR, and stop.

The CA will review your PR.
```

---

# Historical Task — Story Generation Pipeline (COMPLETED, preserved)

> The record below is the original completed task for BA-006. It is preserved unchanged. Do not modify or erase it.

---

## Historical Role

Story Generation Engineer

---

## Historical Module

M08 AI Engine

---

## Historical Status

✅ Complete

---

## Assigned Tasks (P5 - Support for BA-001)

| Task ID | Description | Dependencies | Priority | Status |
|---------|-------------|--------------|----------|--------|
| N/A | Story Generation Pipeline | T-042 | P5 | ✅ Completed |

---

## Files

- `backend/ai/story_generation/`

---

## Constraints

- Depends on BA-001 LLM integration
- Story streaming
- Scene segmentation
- Context management

---

## Acceptance Criteria

- [x] Story generation prompts
- [x] Scene segmentation
- [x] Streaming support
- [x] Context window management

---

## Implementation Summary

### Module Structure
Created `backend/ai/story_generation/` with the following components:

1. **types.py** - Type definitions for:
   - StoryGenre, StoryLength, StoryGenerationConfig
   - GeneratedStory, StoryPromptContext
   - StreamEvent for streaming events
   - Reuses StoryScene, Story from `backend/ai/routing/story/types.py` (BA-001)

2. **prompts.py** - Story generation prompts:
   - Genre-specific templates (Adventure, Fantasy, SciFi, Mystery)
   - System prompts for AI storytelling
   - User prompt builders with context injection

3. **segmenter.py** - Scene segmentation:
   - Scene boundary detection (paragraphs, markers like ***)
   - Character extraction from dialogue
   - Setting and mood detection
   - Scene metadata generation

4. **streamer.py** - Streaming support:
   - Character buffering for performance
   - Scene event emission (scene_start, scene_text, scene_complete)
   - Cancellation support
   - Listener pattern for real-time updates

5. **context.py** - Context window management:
   - Token counting and budgeting
   - Memory summary generation
   - Scene context tracking
   - Context truncation for long stories

6. **generator.py** - Main story generator:
   - Multi-scene story generation
   - Streaming and non-streaming modes
   - Integration with LLM client from BA-001
   - Scene-by-scene generation with context

### Tests
- 102 unit tests covering all modules
- All tests passing

---

## OpenHands Prompt

```
You are Backend & AI Engineer BA-006 working on Story Generation Pipeline.

Read documentation first:
- README.md
- docs/architecture/ARCHITECTURE.md
- docs/modules/MODULES.md
- docs/tasks/TASKS.md
- docs/interfaces/INTERFACES.md
- docs/architecture/DEPENDENCIES.md

This role supports BA-001 with story generation

Dependencies: BA-001 must complete T-042 first

Implementation rules:
1. Story generation prompts
2. Scene segmentation
3. Streaming support
4. Never modify other modules
```

---

## Last Updated

2026-08-03 - Story Generation Pipeline Implementation Complete
