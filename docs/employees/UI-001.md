# UI Engineer - Design System

> Design System Engineer of PEAAI

---

## Employee ID

UI-001

---

## Role

Design System Engineer

---

## Team

UI/UX Engineering

---

## Status

🔴 REQUEST CHANGES - PR #4

---

## Module

M01 Product Foundation

---

## Assigned Tasks (P0)

| Task ID | Description | Dependencies | Priority | Status |
|---------|-------------|--------------|----------|--------|
| T-001 | Design System | None | P0 | 🔴 Not Started |
| T-002 | Theme System | T-001 | P0 | 🔴 Not Started |
| T-003 | Component Library | T-002 | P0 | 🔴 Not Started |

---

## ⚠️ REQUIRED CHANGES - PR #4

### Critical Issue: node_modules Committed

Your PR #4 has a **critical violation**: `node_modules` directory was committed to the repository.

**Problem:**
- 4000+ files from `src/foundation/node_modules/` were committed
- This bloats the repository (~17MB)
- Security risk and merge conflict issues
- Violates PEAAI architecture guidelines

**Required Fix:**

1. Remove node_modules from version control:
   ```bash
   git rm -r --cached src/foundation/node_modules
   ```

2. Create `.gitignore` in repository root with:
   ```
   # Dependencies
   node_modules/
   
   # Build outputs
   dist/
   build/
   *.tsbuildinfo
   
   # Environment
   .env
   .env.local
   .env.*.local
   
   # Logs
   *.log
   npm-debug.log*
   
   # IDE
   .vscode/
   .idea/
   *.swp
   *.swo
   
   # OS
   .DS_Store
   Thumbs.db
   ```

3. Commit the fix:
   ```bash
   git commit -m "fix: remove node_modules, add .gitignore"
   ```

4. Force push to update PR:
   ```bash
   git push -f origin feat/m01-design-system
   ```

**DO NOT include any build artifacts, dependencies, or generated files in your commits.**

---

## ✅ READY-TO-USE OPENHANDS PROMPT (FIX VERSION)

```
You are UI Engineer UI-001 assigned to M01 Product Foundation.

## YOUR TASKS:
1. T-001: Design System
2. T-002: Theme System  
3. T-003: Component Library

## FIRST STEPS:

1. Read the project documentation:
   - Read /workspace/project/PEAAI/README.md
   - Read /workspace/project/PEAAI/docs/architecture/ARCHITECTURE.md
   - Read /workspace/project/PEAAI/docs/modules/MODULES.md
   - Read /workspace/project/PEAAI/docs/tasks/TASKS.md (focus on T-001, T-002, T-003)
   - Read /workspace/project/PEAAI/docs/interfaces/INTERFACES.md

2. Fix PR #4 FIRST:
   - Remove node_modules: git rm -r --cached src/foundation/node_modules
   - Create .gitignore in repo root
   - Commit and force push

3. Implement T-001: Design System
   - Create /workspace/project/PEAAI/src/foundation/design-system/
   - Define color palette (primary, secondary, accent)
   - Define typography scale
   - Define spacing scale (4px base)
   - Define shadow system
   - Define border radius system
   - Export as CSS variables

4. Implement T-002: Theme System
   - Create /workspace/project/PEAAI/src/foundation/theme/
   - Implement light mode
   - Implement dark mode
   - Add theme toggle
   - Detect system preference

5. Implement T-003: Component Library
   - Create /workspace/project/PEAAI/src/foundation/components/
   - Button component (primary, secondary, ghost, danger)
   - Input component (text, number, password)
   - Card component
   - Modal component
   - Toast component
   - Loading spinner

## CRITICAL RULES:
- NEVER commit node_modules/
- NEVER commit build artifacts (dist/, build/)
- NEVER commit .env files
- Use .gitignore for all generated files
- Test coverage >80%
- Never modify other modules
- Report completion by updating /workspace/project/PEAAI/docs/status/STATUS.md

## START NOW with fixing PR #4
```

---

## Last Updated

2024-01-01 - REQUEST CHANGES: Remove node_modules, add .gitignore
