# Backend & AI Engineer - Backend API

> Backend API Engineer of PEAAI

---

## Employee ID

BA-003

---

## Role

Backend API Engineer

---

## Team

Backend & AI Engineering

---

## Status

🔴 REQUEST CHANGES - PR #10

---

## Module

M09 Backend Infrastructure

---

## Assigned Tasks

| Task ID | Description | Priority | Status |
|---------|-------------|----------|--------|
| T-047 | REST API | P4 | ✅ Completed |
| T-048 | Authentication | P4 | ✅ Completed |

---

## ⚠️ REQUIRED CHANGES - PR #10

### Issue 1: __pycache__ Directories Committed

**CRITICAL:** Python bytecode cache directories were committed to the repository.

**Directories committed:**
```
backend/api/__pycache__/
backend/api/routers/__pycache__/
backend/api/middleware/__pycache__/
backend/api/schemas/__pycache__/
backend/auth/__pycache__/
```

**Required Fix:**

```bash
# Remove all __pycache__ directories from version control
git rm -r --cached backend/api/__pycache__
git rm -r --cached backend/api/routers/__pycache__
git rm -r --cached backend/api/middleware/__pycache__
git rm -r --cached backend/api/schemas/__pycache__
git rm -r --cached backend/auth/__pycache__
```

### Issue 2: Missing Python Entries in .gitignore

The `.gitignore` file is missing Python-specific entries.

**Required Fix:**

Add the following to `.gitignore`:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv/

# PyInstaller
*.manifest
*.spec

# Unit test / coverage
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
```

### Issue 3: Duplicate JWT Code

**CRITICAL:** JWT functions are duplicated in two files:

| File | Lines | Functions |
|------|-------|-----------|
| `backend/api/dependencies.py` | 30-63 | `create_access_token`, `create_refresh_token`, `verify_token` |
| `backend/auth/jwt_handler.py` | 11-119 | `create_access_token`, `create_refresh_token`, `verify_token`, `create_password_reset_token` |

**Required Fix:**

1. **Remove duplicate functions from `backend/api/dependencies.py`:**

Delete lines 30-63 (keep only `get_db`, `get_current_user`, `get_current_active_user`)

2. **Add imports at the top of `dependencies.py`:**

```python
from backend.auth.jwt_handler import (
    create_access_token,
    create_refresh_token,
    verify_token,
)
```

3. **Update routers to import JWT functions from `backend.auth.jwt_handler`:**

In `backend/api/routers/auth.py`, remove the local imports and use:

```python
from backend.auth.jwt_handler import (
    create_access_token,
    create_refresh_token,
    verify_token,
    create_password_reset_token,
)
from backend.auth.password import verify_password, hash_password, is_password_strong
```

---

## ✅ COMPLETE FIX GUIDE

Run these commands in order:

```bash
# Step 1: Remove __pycache__ directories
git rm -r --cached backend/api/__pycache__
git rm -r --cached backend/api/routers/__pycache__
git rm -r --cached backend/api/middleware/__pycache__
git rm -r --cached backend/api/schemas/__pycache__
git rm -r --cached backend/auth/__pycache__

# Step 2: Update .gitignore (append to existing file)
cat >> .gitignore << 'EOF'

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
venv/
ENV/
env/
.venv/
*.manifest
*.spec
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
EOF

# Step 3: Fix dependencies.py - Remove duplicate JWT functions
# Delete the following from backend/api/dependencies.py:
# - create_access_token (lines 30-40)
# - create_refresh_token (lines 42-48)
# - verify_token (lines 51-63)
# 
# Then add these imports at the top of the file (after existing imports):
from backend.auth.jwt_handler import (
    create_access_token,
    create_refresh_token,
    verify_token,
)

# Step 4: Commit and push
git add -A
git commit -m "fix: remove __pycache__, fix duplicate JWT code

- Remove all __pycache__ directories from version control
- Add Python-specific entries to .gitignore
- Remove duplicate JWT functions from dependencies.py
- Import JWT functions from backend.auth.jwt_handler"
git push origin feature/backend-api-auth
```

---

## ✅ OPENHANDS PROMPT (FIX VERSION)

```
You are Backend & AI Engineer BA-003.

## YOUR TASK: Fix PR #10 Issues

PR #10 has 3 issues that must be fixed before approval.

### Step 1: Remove __pycache__ from version control
git rm -r --cached backend/api/__pycache__
git rm -r --cached backend/api/routers/__pycache__
git rm -r --cached backend/api/middleware/__pycache__
git rm -r --cached backend/api/schemas/__pycache__
git rm -r --cached backend/auth/__pycache__

### Step 2: Add Python entries to .gitignore
Append the following to .gitignore:
__pycache__/
*.py[cod]
*$py.class
venv/
.env
.venv/

### Step 3: Remove duplicate JWT code from dependencies.py
The functions create_access_token, create_refresh_token, verify_token 
exist in BOTH dependencies.py AND auth/jwt_handler.py.

DELETE these from backend/api/dependencies.py:
- create_access_token function
- create_refresh_token function  
- verify_token function

ADD this import at the top of dependencies.py:
from backend.auth.jwt_handler import (
    create_access_token,
    create_refresh_token,
    verify_token,
)

### Step 4: Commit and push
git add -A
git commit -m "fix: remove __pycache__, fix duplicate JWT code"
git push origin feature/backend-api-auth

## RULES:
- NEVER commit __pycache__/
- NEVER commit .pyc files
- NEVER duplicate code across files
- Use imports instead of copying functions
```

---

## Last Updated

2024-01-01 - Initial document created
2026-08-01 - REQUEST CHANGES: __pycache__ and duplicate code issues
